/**
 * Default Parallel Route for @modal Slot
 *
 * Returns null when no modal route is active
 * Required by Next.js parallel routes to prevent 404 errors
 */
export default function Default() {
  return null;
}
